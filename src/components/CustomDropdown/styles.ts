import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  titleText: {
    alignSelf: 'flex-start',
  },
  errorText: {
    marginTop: '1%',
    alignSelf: 'center',
    color: 'red',
  },
});
